# Command to run script
# ngrok start --config ngrok.yml --all 

version: 2
authtoken: [ENTER_AUTH_TOKEN]
tunnels:
  first:
    proto: http
    addr: 8080
  second:
    proto: http
    addr: 3000
